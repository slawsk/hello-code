# Create the List to Process

Convert CodeDictionary.txt from XML to just a list of text strings, ```convert-dict.py```

Remove the repealed sections and create the list of dictionaries, ```remove_repealed.py```

The list of text strings is ```irc_text_list_with_titles.json```

```pull-random.py``` loads up the json file and picks a random section
